# 每个 Compose 栈自己的插值与运行时环境文件
**/.env
**/runtime.env
**/*.runtime.env
**/.env.next.*
**/.secrets/

# 首次初始化使用的真实值清单；示例文件 config/env.example.json 仍然跟踪
config/env.json

# Traefik 运行时证书状态
infrastructure/traefik/state/acme/*
!infrastructure/traefik/state/acme/.gitkeep

# 运维脚本的主机级部署锁
.ops.lock

# 旧版本部署流程遗留文件；服务器迁移完成后可以删除
.rollback_digest_*
